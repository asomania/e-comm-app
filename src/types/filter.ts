type FilterOptions = {
  filters: {
    min_price: number;
    max_price: number;
    category: string;
    start_date: string;
    end_date: string;
  };
};
export type typeofFilters = FilterOptions;
